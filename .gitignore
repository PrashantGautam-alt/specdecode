venv/
__pycache__/
*.pyc
.cache/

# Trained model weights — large binaries, regenerated by training; never commit
*.pt

# Private interview-defense report — for my eyes only, never publish to GitHub
INTERVIEW_PREP.md
INTERVIEW_PREP.tex
